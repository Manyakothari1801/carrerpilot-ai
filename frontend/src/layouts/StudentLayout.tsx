import { NavLink, Outlet, useNavigate } from 'react-router-dom'
import { useAuth } from '../features/auth/AuthContext'
import { Logo } from '../pages/AuthPage'

export function StudentLayout() { const { user, logout } = useAuth(); const navigate = useNavigate(); const signOut = async (all = false) => { await logout(all); navigate('/login') }; return <div className="min-h-screen bg-slate-950 text-white lg:grid lg:grid-cols-[260px_1fr]"><aside className="border-b border-white/10 bg-slate-900/70 p-6 lg:min-h-screen lg:border-b-0 lg:border-r"><Logo /><nav className="mt-10 flex flex-wrap gap-2 lg:flex-col"><NavLink className={({ isActive }) => `nav-link ${isActive ? 'nav-link-active' : ''}`} to="/student/dashboard">Overview</NavLink><NavLink className={({ isActive }) => `nav-link ${isActive ? 'nav-link-active' : ''}`} to="/student/profile">Profile</NavLink><NavLink className={({ isActive }) => `nav-link ${isActive ? 'nav-link-active' : ''}`} to="/student/resumes">Resumes</NavLink><NavLink className={({ isActive }) => `nav-link ${isActive ? 'nav-link-active' : ''}`} to="/student/job-match">Job Match</NavLink></nav></aside><div><header className="flex items-center justify-between border-b border-white/10 px-6 py-4 lg:px-10"><div><p className="text-xs uppercase tracking-widest text-slate-500">Student workspace</p><p className="font-medium">{user?.fullName}</p></div><div className="flex gap-2"><button className="secondary-button" onClick={() => void signOut(true)}>Logout all</button><button className="secondary-button" onClick={() => void signOut()}>Logout</button></div></header><Outlet /></div></div> }
