# Feature boundaries

Feature folders are introduced only when their implementation phase begins. Planned boundaries mirror the backend: authentication, profile, resumes, job matching, skill gaps, roadmaps, assessments, exams, proctoring, analytics, reports, notifications, administration, and the career advisor.

Each feature may own its components, routes, hooks, API functions, schemas, and types. Shared visual primitives remain outside this directory.
