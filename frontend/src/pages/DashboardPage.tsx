import { useQuery } from '@tanstack/react-query'
import { Link } from 'react-router-dom'
import { motion } from 'framer-motion'
import { api } from '../services/api'
import type { Profile } from '../features/profile/types'

const actions=[['Job Match','Compare your profile with a role'],['Skill Gap','Find the skills to learn next'],['Assessments','Practice with tailored questions']]
export function DashboardPage(){const{data}=useQuery({queryKey:['profile'],queryFn:async()=>(await api.get<Profile>('/profile')).data});return <main className="page"><p className="eyebrow">Your command center</p><h1 className="page-title">Good to see you, {data?.fullName?.split(' ')[0]??'Pilot'}.</h1><p className="page-subtitle">Complete your profile and prepare a resume for future career intelligence.</p><section className="mt-8 grid gap-5 md:grid-cols-[1.3fr_.7fr]"><div className="card"><div className="flex justify-between"><div><p className="text-sm text-slate-400">Profile completion</p><p className="mt-2 text-4xl font-semibold">{data?.profileCompletionPercentage??0}%</p></div><Link className="text-sm text-cyan-300" to="/student/profile">Improve profile →</Link></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-cyan-400" style={{width:`${data?.profileCompletionPercentage??0}%`}}/></div><p className="mt-4 text-sm text-slate-400">{data?.missingFields.length?`Next: ${data.missingFields.slice(0,3).join(', ')}`:'Your profile foundation is complete.'}</p></div><Link to="/student/resumes" className="card bg-gradient-to-br from-cyan-400/15 to-indigo-500/10 transition hover:border-cyan-300/30"><p className="text-sm text-cyan-200">Resume Manager</p><h2 className="mt-3 text-xl font-semibold">Upload and prepare your resume</h2><p className="mt-2 text-sm text-slate-400">Store, parse, review, and choose the active resume for later analysis.</p></Link></section><h2 className="mt-10 text-xl font-semibold">Coming next</h2><section className="mt-4 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{actions.map(([title,copy],index)=><motion.article initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:index*.05}} className="card opacity-75" key={title}><span className="pill">Coming soon</span><h3 className="mt-5 font-semibold">{title}</h3><p className="mt-2 text-sm text-slate-400">{copy}</p></motion.article>)}</section></main>}
