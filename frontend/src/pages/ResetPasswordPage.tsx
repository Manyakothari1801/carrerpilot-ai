import { useState, type FormEvent } from 'react'
import { Link, useSearchParams } from 'react-router-dom'
import { api } from '../services/api'
import { AuthShell } from './AuthPage'

export function ResetPasswordPage() { const [params] = useSearchParams(); const [token, setToken] = useState(params.get('token') ?? ''); const [password, setPassword] = useState(''); const [confirmPassword, setConfirm] = useState(''); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); try { const { data } = await api.post('/auth/reset-password', { token, password, confirmPassword }); setMessage(data.message) } catch { setError('Reset token or password is invalid') } }; return <AuthShell title="Choose a new password" subtitle="Use at least eight characters with upper-case, lower-case, and a number."><form onSubmit={submit} className="space-y-4"><label className="block text-sm">Reset token<input className="input mt-2" required value={token} onChange={event => setToken(event.target.value)} /></label><label className="block text-sm">New password<input className="input mt-2" required type="password" value={password} onChange={event => setPassword(event.target.value)} /></label><label className="block text-sm">Confirm password<input className="input mt-2" required type="password" value={confirmPassword} onChange={event => setConfirm(event.target.value)} /></label>{error && <p className="error-message">{error}</p>}{message && <p className="success-message">{message}</p>}<button className="primary-button w-full">Reset password</button></form><Link className="mt-5 block text-sm text-cyan-300" to="/login">Back to sign in</Link></AuthShell> }
