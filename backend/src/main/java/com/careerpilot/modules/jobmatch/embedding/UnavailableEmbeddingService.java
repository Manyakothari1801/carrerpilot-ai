package com.careerpilot.modules.jobmatch.embedding;import com.careerpilot.config.EmbeddingProperties;import org.springframework.stereotype.Service;import java.util.Optional;@Service public class UnavailableEmbeddingService implements EmbeddingService{private final EmbeddingProperties properties;public UnavailableEmbeddingService(EmbeddingProperties properties){this.properties=properties;}public Optional<double[]>embed(String text){return Optional.empty();}public String provider(){return properties.enabled()?properties.provider():"DISABLED";}}
