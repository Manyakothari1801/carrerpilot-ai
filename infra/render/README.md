# Render

Backend deployment configuration will be added during the deployment phase. The service must use environment variables and a managed PostgreSQL connection; local filesystem persistence is not supported.

